# Runtime dependencies for AWS Elastic Beanstalk (Amazon Linux).
# The deployed model is a sklearn LinearRegression and the preprocessor is a
# sklearn ColumnTransformer, so catboost/xgboost are NOT needed to serve
# predictions (they are only used during training). Keeping them out avoids
# a ~500 MB install that overflows the t3.micro disk.
# For the full training/notebook environment, see requirements-dev.txt.
Flask==3.0.3
gunicorn==23.0.0
numpy==1.24.4
pandas==2.0.3
scikit-learn==1.3.2
scipy==1.10.1
dill==0.4.0
